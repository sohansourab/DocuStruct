pytesseract>=0.3.10
Pillow>=10.0.0
# Optional, only needed if you swap in LocalLLMExtractor.
# Verified building/installing cleanly from source as llama-cpp-python==0.3.33 (CPU-only).
# llama-cpp-python>=0.2.0
